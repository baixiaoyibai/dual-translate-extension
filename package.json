{
  "name": "dual-translate-extension",
  "version": "1.0.5",
  "description": "多模式网页翻译扩展，支持英语/日语翻译为简体中文，提供双语对照、仅译文、对照面板等多种显示模式",
  "main": "background.js",
  "scripts": {
    "lint": "echo \"[TODO] ESLint 待配置（阶段二）\" && exit 0",
    "test": "echo \"[TODO] 测试待添加（阶段二）\" && exit 0",
    "check": "node --check background.js && node --check content.js && node --check lib/api-manager.js && node --check lib/settings-manager.js && node --check lib/translation-cache.js && node --check lib/logger.js && node --check lib/escape-utils.js && node --check lib/api-adapters/baidu.js && node --check lib/api-adapters/baidu-llm.js && node --check lib/api-adapters/llm-generic.js && node --check popup/popup.js && node --check options/options.js"
  },
  "keywords": [
    "edge-extension",
    "chrome-extension",
    "manifest-v3",
    "translation",
    "bilingual",
    "game-mod"
  ],
  "author": "TODO: 替换为你的 GitHub 用户名",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "TODO: 替换为你的 GitHub 仓库地址"
  },
  "engines": {
    "node": ">=18"
  }
}
